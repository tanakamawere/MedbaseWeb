@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

@if (Model?.ShowRequestId ?? false)
{
    <p>
        <strong>Request ID:</strong> <code>@Model?.RequestId</code>
    </p>
}
<div class="text-center">
                <img class="img-fluid rounded-3 my-5"
                     src="~/img/error.svg" alt="..." />
</div>
